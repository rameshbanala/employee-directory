{
  "name": "employee-directory",
  "version": "1.0.0",
  "description": "Employee Directory for AJACKUS",
  "scripts": {
    "start": "live-server . --port=8080",
    "lint": "eslint js/",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "live-server": "^1.2.2",
    "prettier": "^3.2.5"
  }
}
